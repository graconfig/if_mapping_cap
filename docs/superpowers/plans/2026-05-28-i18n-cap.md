# CAP 后端多语言改造实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 if_mapping_cap 添加中日文多语言支持，使错误消息和面向用户的日志消息根据请求的 `language` 参数动态返回。

**Architecture:** 新增 `srv/utils/i18n.ts` 提供 `t(key, lang, vars?)` 函数，读取 `srv/i18n/messages_<lang>.json` 翻译文件（lazy load + 内存缓存）。`Errors` 工厂接受 `lang` 参数，pipeline 各步骤的用户可见日志通过 `t()` 国际化。内部技术日志保持英文。

**Tech Stack:** TypeScript, Node.js `fs.readFileSync`, CAP `@sap/cds`, Jest

---

## 文件变更清单

| 操作 | 路径 | 说明 |
|------|------|------|
| 新增 | `srv/i18n/messages_zh.json` | 中文翻译 |
| 新增 | `srv/i18n/messages_ja.json` | 日语翻译 |
| 新增 | `srv/utils/i18n.ts` | t() 函数模块 |
| 新增 | `tests/utils/i18n.test.ts` | i18n 单元测试 |
| 修改 | `srv/utils/errors.ts` | Errors 工厂接受 lang 参数 |
| 修改 | `tests/utils/errors.test.ts` | 更新测试适配新签名 |
| 修改 | `srv/if-mapping-service.ts` | 错误调用传 lang，启动日志国际化 |
| 修改 | `srv/matching/orchestrator.ts` | 完成日志国际化 |
| 修改 | `srv/matching/step1-custom-fields.ts` | 完成日志国际化 |
| 修改 | `srv/ai/prompt-manager.ts` | PROMPT_NOT_FOUND 调用适配新签名 |

---

## Task 1: 创建翻译文件

**Files:**
- Create: `srv/i18n/messages_zh.json`
- Create: `srv/i18n/messages_ja.json`

- [ ] **Step 1: 创建中文翻译文件**

```json
// srv/i18n/messages_zh.json
{
  "error.hana_connection": "HANA 连接失败",
  "error.ai_core_auth": "AI Core 认证失败",
  "error.ai_core_timeout": "AI Core 请求超时",
  "error.invalid_provider": "未知的 Provider: {p}",
  "error.prompt_not_found": "未找到激活的 Prompt（step=\"{step}\" language=\"{lang}\"）",
  "error.internal": "内部服务器错误",
  "error.upload_failed": "上传失败",
  "log.service_bootstrapped": "服务启动完成",
  "log.step1_complete": "Step1 完成",
  "log.orchestrator_complete": "匹配完成"
}
```

- [ ] **Step 2: 创建日语翻译文件**

```json
// srv/i18n/messages_ja.json
{
  "error.hana_connection": "HANA 接続に失敗しました",
  "error.ai_core_auth": "AI Core 認証に失敗しました",
  "error.ai_core_timeout": "AI Core リクエストがタイムアウトしました",
  "error.invalid_provider": "不明な Provider: {p}",
  "error.prompt_not_found": "ステップ \"{step}\" 言語 \"{lang}\" のアクティブなプロンプトが見つかりません",
  "error.internal": "内部サーバーエラー",
  "error.upload_failed": "アップロードに失敗しました",
  "log.service_bootstrapped": "サービス起動完了",
  "log.step1_complete": "Step1 完了",
  "log.orchestrator_complete": "マッチング完了"
}
```

- [ ] **Step 3: Commit**

```bash
git add srv/i18n/messages_zh.json srv/i18n/messages_ja.json
git commit -m "feat: add i18n translation files for zh and ja"
```

---

## Task 2: 创建 i18n 模块

**Files:**
- Create: `srv/utils/i18n.ts`
- Create: `tests/utils/i18n.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
// tests/utils/i18n.test.ts
import { t } from '../../srv/utils/i18n';

test('returns key as fallback when key not found', () => {
  expect(t('nonexistent.key', 'zh')).toBe('nonexistent.key');
});

test('returns zh translation', () => {
  expect(t('error.hana_connection', 'zh')).toBe('HANA 连接失败');
});

test('returns ja translation', () => {
  expect(t('error.hana_connection', 'ja')).toBe('HANA 接続に失敗しました');
});

test('interpolates {placeholder} variables', () => {
  expect(t('error.invalid_provider', 'zh', { p: 'grok' })).toBe('未知的 Provider: grok');
});

test('interpolates multiple placeholders in ja', () => {
  expect(t('error.prompt_not_found', 'ja', { step: 'view_selection', lang: 'en' }))
    .toBe('ステップ "view_selection" 言語 "en" のアクティブなプロンプトが見つかりません');
});

test('falls back to ja when unknown lang requested', () => {
  const result = t('error.hana_connection', 'fr');
  expect(result).toBe('HANA 接続に失敗しました');
});
```

- [ ] **Step 2: 验证测试失败**

```bash
npx jest tests/utils/i18n.test.ts --runInBand
```

期望: FAIL — `Cannot find module '../../srv/utils/i18n'`

- [ ] **Step 3: 实现 i18n 模块**

```typescript
// srv/utils/i18n.ts
import { readFileSync } from 'fs';
import { join } from 'path';

type Messages = Record<string, string>;

const _cache = new Map<string, Messages>();

function load(lang: string): Messages {
  if (_cache.has(lang)) return _cache.get(lang)!;
  const file = join(process.cwd(), 'srv', 'i18n', `messages_${lang}.json`);
  try {
    const msgs = JSON.parse(readFileSync(file, 'utf-8')) as Messages;
    _cache.set(lang, msgs);
    return msgs;
  } catch {
    if (lang !== 'ja') return load('ja');
    return {};
  }
}

export function t(key: string, lang: string, vars?: Record<string, string>): string {
  const msgs = load(lang);
  let text = msgs[key] ?? key;
  if (vars) {
    for (const [k, v] of Object.entries(vars)) {
      text = text.split(`{${k}}`).join(v);
    }
  }
  return text;
}
```

- [ ] **Step 4: 验证测试通过**

```bash
npx jest tests/utils/i18n.test.ts --runInBand
```

期望: 6 tests PASS

- [ ] **Step 5: Commit**

```bash
git add srv/utils/i18n.ts tests/utils/i18n.test.ts
git commit -m "feat: add i18n t() module with zh/ja message files"
```

---

## Task 3: 更新 Errors 工厂接受 lang 参数

**Files:**
- Modify: `srv/utils/errors.ts`
- Modify: `tests/utils/errors.test.ts`

- [ ] **Step 1: 查看当前 errors.test.ts（已在上文读取，此步骤确认签名）**

当前测试调用：
- `Errors.HANA_CONNECTION()` — 无参数
- `Errors.INVALID_PROVIDER('unknown')` — 一个参数
- `Errors.PROMPT_NOT_FOUND('view_selection', 'ja')` — 两个参数

新签名设计：所有工厂默认 `lang = 'ja'`，`PROMPT_NOT_FOUND` 新增第三个可选参数 `uiLang`（默认等于 `promptLang`），确保现有调用签名完全向后兼容，**现有测试无需修改**。

- [ ] **Step 2: 更新 errors.ts**

```typescript
// srv/utils/errors.ts
import { t } from './i18n.js';

export class AppError extends Error {
  constructor(
    message: string,
    public readonly statusCode: number
  ) {
    super(message);
    this.name = 'AppError';
  }
}

export const Errors = {
  HANA_CONNECTION:  (lang = 'ja'): AppError =>
    new AppError(t('error.hana_connection', lang), 503),
  AI_CORE_AUTH:     (lang = 'ja'): AppError =>
    new AppError(t('error.ai_core_auth', lang), 502),
  AI_CORE_TIMEOUT:  (lang = 'ja'): AppError =>
    new AppError(t('error.ai_core_timeout', lang), 504),
  INVALID_PROVIDER: (p: string, lang = 'ja'): AppError =>
    new AppError(t('error.invalid_provider', lang, { p }), 400),
  PROMPT_NOT_FOUND: (step: string, promptLang: string, uiLang = promptLang): AppError =>
    new AppError(t('error.prompt_not_found', uiLang, { step, lang: promptLang }), 500),
};
```

- [ ] **Step 3: 运行 errors 测试验证向后兼容**

```bash
npx jest tests/utils/errors.test.ts --runInBand
```

期望: 3 tests PASS（现有测试无需修改，因为占位符值 `HANA`、`unknown`、`view_selection`、`ja` 都出现在翻译结果字符串中）

- [ ] **Step 4: 运行全套测试确认无回归**

```bash
npm test
```

期望: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add srv/utils/errors.ts
git commit -m "feat: i18n Errors factory — accept lang param, default ja"
```

---

## Task 4: 更新 prompt-manager.ts 调用签名

**Files:**
- Modify: `srv/ai/prompt-manager.ts`

`PromptManager.getPrompt()` 内调用 `Errors.PROMPT_NOT_FOUND(step, language)` — `language` 在此处是 prompt 语言，也即 `uiLang`，二者相同，所以不需要传第三个参数，签名向后兼容，此处只验证无需修改即可通过。

- [ ] **Step 1: 验证 prompt-manager 测试仍通过**

```bash
npx jest tests/ai/prompt-manager.test.ts --runInBand
```

期望: PASS（`Errors.PROMPT_NOT_FOUND(step, language)` 两参数调用与新签名完全兼容）

---

## Task 5: 国际化服务层日志和错误消息

**Files:**
- Modify: `srv/if-mapping-service.ts`

在 `if-mapping-service.ts` 中：
- `log.info('Service bootstrapped')` → 使用 `t()`（默认语言 `ja`，因启动时无请求上下文）
- `req.error(500, 'Internal server error')` → `t('error.internal', config.language)`
- `req.error(500, 'Upload failed')` → `t('error.upload_failed', config.language)`
- `Errors.HANA_CONNECTION()` → `Errors.HANA_CONNECTION(config.language)`（仅 match handler 内部有 config）
- `Errors.AI_CORE_*` → 在 orchestrator/steps 中处理

注意：`uploadCustomFields` handler 没有 `config` 对象（无语言参数），使用默认 `'ja'`。

- [ ] **Step 1: 更新 if-mapping-service.ts**

```typescript
// srv/if-mapping-service.ts — 完整替换
import cds from '@sap/cds';
import type { Request as ExpressRequest, Response as ExpressResponse } from 'express';
import { HanaRepository } from './repository/hana-repository.js';
import { AiCoreClient } from './ai/aicore-client.js';
import { promptManager } from './ai/prompt-manager.js';
import { runMatching, OrchestratorDeps } from './matching/orchestrator.js';
import type { InterfaceFieldInput } from '../@cds-models/index.js';
import { buildRequestConfig } from './utils/config.js';
import { AppError } from './utils/errors.js';
import { t } from './utils/i18n.js';
import { log } from './utils/logger.js';
import { logBus } from './utils/log-bus.js';
import type { LogEntry } from './utils/log-bus.js';

const hana   = new HanaRepository();
const aiCore = new AiCoreClient();
const deps: OrchestratorDeps = { hana, aiCore, prompts: promptManager };

// SSE log stream — clients subscribe with ?correlationId=<id>
cds.on('served', () => {
  const app = (cds as any).app;
  app.get('/log-stream', (req: ExpressRequest, res: ExpressResponse) => {
    const correlationId = req.query['correlationId'] as string | undefined;
    if (!correlationId) {
      res.status(400).end('correlationId required');
      return;
    }
    res.setHeader('Content-Type',  'text/event-stream');
    res.setHeader('Cache-Control', 'no-cache');
    res.setHeader('Connection',    'keep-alive');
    res.flushHeaders();

    const onLog = (entry: LogEntry) => {
      res.write(`data: ${JSON.stringify(entry)}\n\n`);
    };
    logBus.on(correlationId, onLog);
    req.on('close', () => logBus.off(correlationId, onLog));
  });
});

cds.on('shutdown', async () => {
  await hana.disconnect();
});

module.exports = class IfMappingService extends cds.ApplicationService {
  async init() {
    await super.init();
    await hana.connect();
    await promptManager.initialize();
    log.info(t('log.service_bootstrapped', 'ja'));

    this.on('match', async (req) => {
      const { fields, provider, language } = req.data as {
        fields:    InterfaceFieldInput[];
        provider?: string;
        language?: string;
      };
      const config = buildRequestConfig(provider ?? 'claude', language ?? 'ja');
      const correlationId = (req as any).headers?.['x-correlation-id'] as string | undefined;

      try {
        const results = await runMatching(fields, config, deps, correlationId);
        return results;
      } catch (err) {
        log.error('match action failed', { correlationId, error: String(err) });
        if (err instanceof AppError) {
          return req.error(err.statusCode, err.message);
        }
        return req.error(500, t('error.internal', config.language));
      }
    });

    this.on('uploadCustomFields', async (req) => {
      const { records, mode } = req.data as {
        records: import('./repository/hana-repository.js').CustomFieldRecord[];
        mode?:   string;
      };
      try {
        const result = mode === 'overwrite'
          ? await hana.overwriteCustomFields(records)
          : await hana.upsertCustomFields(records);
        return result;
      } catch (err) {
        log.error('uploadCustomFields failed', { error: String(err) });
        return req.error(500, t('error.upload_failed', 'ja'));
      }
    });

    this.on('reloadPrompts', async (_req) => {
      await promptManager.reload();
      return { success: true };
    });
  }
};
```

- [ ] **Step 2: 运行服务层测试**

```bash
npx jest tests/service.test.ts tests/service-handler.test.ts --runInBand
```

期望: PASS

- [ ] **Step 3: Commit**

```bash
git add srv/if-mapping-service.ts
git commit -m "feat: i18n service layer — startup log and error messages"
```

---

## Task 6: 国际化 orchestrator 和 step1 日志

**Files:**
- Modify: `srv/matching/orchestrator.ts`
- Modify: `srv/matching/step1-custom-fields.ts`

这两处的 `log.info(...)` 消息通过 SSE 流推送到 GUI，是用户可见的消息，需要国际化。`correlationId` 和 `total` 等技术字段保持在 `context` 对象中不翻译。

- [ ] **Step 1: 更新 orchestrator.ts**

在文件顶部 import 区添加：
```typescript
import { t } from '../utils/i18n.js';
```

将两处 `log.info('Orchestrator complete', ...)` 改为：

```typescript
// orchestrator.ts 第 58 行（全匹配路径）
log.info(t('log.orchestrator_complete', config.language), { correlationId, total: results.length });

// orchestrator.ts 第 105 行（含 step2/3 路径）
log.info(t('log.orchestrator_complete', config.language), { correlationId, total: results.length });
```

- [ ] **Step 2: 更新 step1-custom-fields.ts**

在文件顶部 import 区添加：
```typescript
import { t } from '../utils/i18n.js';
```

将 `log.info('Step1 complete', ...)` 改为：

```typescript
// step1-custom-fields.ts 第 82 行
log.info(t('log.step1_complete', config.language), { correlationId, matched: matched.length, unmatched: stillUnmatched.length });
```

- [ ] **Step 3: 运行匹配流程测试**

```bash
npx jest tests/matching/orchestrator.test.ts tests/matching/step1-custom-fields.test.ts --runInBand
```

期望: PASS

- [ ] **Step 4: 运行全套测试**

```bash
npm test
```

期望: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add srv/matching/orchestrator.ts srv/matching/step1-custom-fields.ts
git commit -m "feat: i18n orchestrator and step1 user-facing log messages"
```

---

## 自查清单

- [x] **Spec 覆盖**: 翻译文件 ✓，i18n 模块 ✓，Errors 工厂 ✓，服务层 ✓，orchestrator ✓，step1 ✓，prompt-manager 兼容性 ✓
- [x] **占位符扫描**: 无 TBD/TODO
- [x] **类型一致性**: `t(key, lang, vars?)` 签名在测试和实现中一致；`Errors.PROMPT_NOT_FOUND(step, promptLang, uiLang?)` 与 prompt-manager.ts 现有调用向后兼容

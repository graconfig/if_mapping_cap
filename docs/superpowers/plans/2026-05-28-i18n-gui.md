# GUI 多语言改造实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 if_mapping_gui 添加中日文 UI 语言支持，将硬编码的中文文本提取到 JSON 翻译文件，通过设置页面切换语言，无需重启即时生效。

**Architecture:** 新增 `i18n/` 目录存放 `zh.json`/`ja.json` 翻译文件，`i18n.py` 提供 `t(key, **kwargs)` 函数。`App` 持有当前语言状态并提供 `retranslate()` 方法逐一刷新各 frame 的静态文本。设置页面新增「界面语言」下拉，保存时调用 `retranslate()`。

**Tech Stack:** Python 3, customtkinter, json, pytest

---

## 文件变更清单

| 操作 | 路径 | 说明 |
|------|------|------|
| 新增 | `i18n/zh.json` | 中文翻译（原始硬编码文本） |
| 新增 | `i18n/ja.json` | 日语翻译 |
| 新增 | `i18n.py` | t() 函数，lazy load + 内存缓存 |
| 新增 | `tests/test_i18n.py` | i18n 单元测试 |
| 修改 | `config.py` | DEFAULTS 中新增 `ui_language: "zh"` |
| 修改 | `gui/frames/__init__.py` | BaseFrame 新增 `retranslate()` 默认方法 |
| 修改 | `gui/app.py` | 启动时加载语言，新增 `retranslate()` |
| 修改 | `gui/frames/match_frame.py` | 所有 UI 文本改用 t()，新增 retranslate() |
| 修改 | `gui/frames/upload_frame.py` | 同上 |
| 修改 | `gui/frames/prompts_frame.py` | 同上 |
| 修改 | `gui/frames/logs_frame.py` | 同上 |
| 修改 | `gui/frames/settings_frame.py` | 新增 UI 语言选项，实现 retranslate() |

---

## Task 1: 翻译文件和 i18n 模块

**Files:**
- Create: `i18n/zh.json`
- Create: `i18n/ja.json`
- Create: `i18n.py`
- Create: `tests/test_i18n.py`

- [ ] **Step 1: 创建中文翻译文件**

```json
// i18n/zh.json
{
  "app.title": "IF Mapping",
  "nav.match": "▶  字段匹配",
  "nav.upload": "⬆  上传知识库",
  "nav.prompts": "📝  Prompt 管理",
  "nav.logs": "📊  Token 日志",
  "nav.settings": "⚙  设置",
  "match.pick_files": "📂  点击选择 Excel 文件（可多选）\n支持 .xlsx / .xls",
  "match.provider_label": "Provider",
  "match.lang_label": "语言",
  "match.start_btn": "▶ 开始匹配",
  "match.stop_btn": "■ 停止",
  "match.export_btn": "📥 导出结果 Excel",
  "match.pick_title": "选择 Excel 文件",
  "match.no_file_error": "[ERROR] 请先选择 Excel 文件",
  "match.parse_log": "解析 {name} — {count} 条字段 [{direction}]",
  "match.match_done_log": "匹配完成: {total} 条 (知识库 {custom} · AI {ai})",
  "match.match_fail_log": "{name} 匹配失败: {error}",
  "match.user_stop_log": "用户停止 — 处理中断",
  "match.conn_fail_log": "无法连接到 {url} — 请检查 CAP 是否已启动",
  "match.result_summary": "完成: {total} 条 ｜ 知识库 {custom} ｜ AI {ai}",
  "match.export_done_log": "已导出: {name}",
  "upload.title": "上传知识库",
  "upload.pick_btn": "📂  点击选择知识库 Excel 文件",
  "upload.mode_label": "上传模式",
  "upload.upload_btn": "⬆ 上传",
  "upload.no_file_error": "[ERROR] 请先选择文件",
  "upload.pick_title": "选择知识库 Excel",
  "upload.conn_fail_log": "无法连接到 {url}",
  "upload.parse_log": "解析 {name} — {count} 条记录",
  "upload.done_log": "上传完成: 插入 {inserted}, 更新 {updated}, 删除 {deleted}",
  "upload.fail_log": "上传失败: {error}",
  "prompts.title": "Prompt 管理",
  "prompts.cancel_btn": "取消",
  "prompts.save_btn": "💾 保存",
  "prompts.reload_btn": "🔄 重载",
  "logs.title": "Token 日志",
  "logs.refresh_btn": "🔄 刷新",
  "logs.total_input": "总 Input Tokens",
  "logs.total_output": "总 Output Tokens",
  "logs.call_count": "调用次数",
  "logs.col_time": "时间",
  "logs.col_provider": "Provider",
  "logs.col_step": "Step",
  "logs.col_input": "Input",
  "logs.col_output": "Output",
  "settings.title": "设置",
  "settings.cap_url_label": "CAP 服务地址",
  "settings.timeout_label": "超时(秒)",
  "settings.test_conn_btn": "🔌 测试连接",
  "settings.testing": "测试中…",
  "settings.connected": "✓ 已连接",
  "settings.disconnected": "✗ 无法连接",
  "settings.provider_label": "默认 Provider",
  "settings.lang_label": "默认语言",
  "settings.ui_lang_label": "界面语言",
  "settings.excel_section": "Excel 列配置",
  "settings.sheet_head": "Sheet（抬头）",
  "settings.sheet_data": "Sheet（数据）",
  "settings.header_row": "抬头行",
  "settings.start_row": "起始行",
  "settings.det_col": "检测列",
  "settings.det_row": "检测行",
  "settings.det_keyword": "关键字",
  "settings.dir_normal": "普通方向",
  "settings.dir_sap": "SAP方向",
  "settings.input_header_cols": "抬头列",
  "settings.input_row_cols": "输入数据列",
  "settings.output_cols": "输出列",
  "settings.save_btn": "💾 保存设置",
  "status.checking": "● 检查中…",
  "status.disconnected": "●  未连接"
}
```

- [ ] **Step 2: 创建日语翻译文件**

```json
// i18n/ja.json
{
  "app.title": "IF Mapping",
  "nav.match": "▶  フィールドマッチング",
  "nav.upload": "⬆  ナレッジベースアップロード",
  "nav.prompts": "📝  プロンプト管理",
  "nav.logs": "📊  トークンログ",
  "nav.settings": "⚙  設定",
  "match.pick_files": "📂  Excel ファイルを選択（複数可）\n.xlsx / .xls 対応",
  "match.provider_label": "Provider",
  "match.lang_label": "言語",
  "match.start_btn": "▶ マッチング開始",
  "match.stop_btn": "■ 停止",
  "match.export_btn": "📥 結果を Excel エクスポート",
  "match.pick_title": "Excel ファイルを選択",
  "match.no_file_error": "[ERROR] Excel ファイルを選択してください",
  "match.parse_log": "{name} を解析 — {count} 件 [{direction}]",
  "match.match_done_log": "マッチング完了: {total} 件（KB {custom} · AI {ai}）",
  "match.match_fail_log": "{name} のマッチングに失敗: {error}",
  "match.user_stop_log": "ユーザーが停止 — 処理を中断しました",
  "match.conn_fail_log": "{url} に接続できません — CAP が起動しているか確認してください",
  "match.result_summary": "完了: {total} 件 ｜ KB {custom} ｜ AI {ai}",
  "match.export_done_log": "エクスポート完了: {name}",
  "upload.title": "ナレッジベースアップロード",
  "upload.pick_btn": "📂  ナレッジベース Excel を選択",
  "upload.mode_label": "アップロードモード",
  "upload.upload_btn": "⬆ アップロード",
  "upload.no_file_error": "[ERROR] ファイルを選択してください",
  "upload.pick_title": "ナレッジベース Excel を選択",
  "upload.conn_fail_log": "{url} に接続できません",
  "upload.parse_log": "{name} を解析 — {count} 件",
  "upload.done_log": "アップロード完了: 挿入 {inserted}, 更新 {updated}, 削除 {deleted}",
  "upload.fail_log": "アップロードに失敗: {error}",
  "prompts.title": "プロンプト管理",
  "prompts.cancel_btn": "キャンセル",
  "prompts.save_btn": "💾 保存",
  "prompts.reload_btn": "🔄 再読み込み",
  "logs.title": "トークンログ",
  "logs.refresh_btn": "🔄 更新",
  "logs.total_input": "合計 Input Tokens",
  "logs.total_output": "合計 Output Tokens",
  "logs.call_count": "呼び出し回数",
  "logs.col_time": "時刻",
  "logs.col_provider": "Provider",
  "logs.col_step": "Step",
  "logs.col_input": "Input",
  "logs.col_output": "Output",
  "settings.title": "設定",
  "settings.cap_url_label": "CAP サービス URL",
  "settings.timeout_label": "タイムアウト(秒)",
  "settings.test_conn_btn": "🔌 接続テスト",
  "settings.testing": "テスト中…",
  "settings.connected": "✓ 接続済み",
  "settings.disconnected": "✗ 接続できません",
  "settings.provider_label": "デフォルト Provider",
  "settings.lang_label": "デフォルト言語",
  "settings.ui_lang_label": "画面言語",
  "settings.excel_section": "Excel 列設定",
  "settings.sheet_head": "シート（ヘッダー）",
  "settings.sheet_data": "シート（データ）",
  "settings.header_row": "ヘッダー行",
  "settings.start_row": "開始行",
  "settings.det_col": "検出列",
  "settings.det_row": "検出行",
  "settings.det_keyword": "キーワード",
  "settings.dir_normal": "通常方向",
  "settings.dir_sap": "SAP方向",
  "settings.input_header_cols": "ヘッダー列",
  "settings.input_row_cols": "入力データ列",
  "settings.output_cols": "出力列",
  "settings.save_btn": "💾 設定を保存",
  "status.checking": "● 確認中…",
  "status.disconnected": "●  未接続"
}
```

- [ ] **Step 3: 创建 i18n.py**

```python
# i18n.py
import json
from pathlib import Path

_FALLBACK = "zh"
_cache: dict[str, dict[str, str]] = {}
_lang = _FALLBACK


def load(lang: str) -> None:
    global _lang
    _lang = lang
    if lang not in _cache:
        path = Path(__file__).parent / "i18n" / f"{lang}.json"
        _cache[lang] = json.loads(path.read_text(encoding="utf-8"))


def t(key: str, **kwargs: str) -> str:
    strings = _cache.get(_lang) or _cache.get(_FALLBACK) or {}
    text = strings.get(key, key)
    return text.format(**kwargs) if kwargs else text


def current() -> str:
    return _lang
```

- [ ] **Step 4: 写 i18n 测试**

```python
# tests/test_i18n.py
import pytest
import i18n


def test_zh_translation():
    i18n.load("zh")
    assert i18n.t("app.title") == "IF Mapping"
    assert i18n.t("nav.match") == "▶  字段匹配"


def test_ja_translation():
    i18n.load("ja")
    assert i18n.t("app.title") == "IF Mapping"
    assert i18n.t("nav.match") == "▶  フィールドマッチング"


def test_interpolation_zh():
    i18n.load("zh")
    result = i18n.t("match.parse_log", name="test.xlsx", count="10", direction="normal")
    assert result == "解析 test.xlsx — 10 条字段 [normal]"


def test_interpolation_ja():
    i18n.load("ja")
    result = i18n.t("match.parse_log", name="test.xlsx", count="10", direction="normal")
    assert result == "test.xlsx を解析 — 10 件 [normal]"


def test_missing_key_returns_key():
    i18n.load("zh")
    assert i18n.t("nonexistent.key") == "nonexistent.key"


def test_current_returns_active_lang():
    i18n.load("ja")
    assert i18n.current() == "ja"
    i18n.load("zh")
    assert i18n.current() == "zh"


def test_all_keys_present_in_both_langs():
    i18n.load("zh")
    zh_keys = set(i18n._cache["zh"].keys())
    i18n.load("ja")
    ja_keys = set(i18n._cache["ja"].keys())
    missing_in_ja = zh_keys - ja_keys
    assert not missing_in_ja, f"Keys missing in ja.json: {missing_in_ja}"
```

- [ ] **Step 5: 运行 i18n 测试验证通过**

```bash
cd d:/Users/PC/Projects/if_mapping_gui
pytest tests/test_i18n.py -v
```

期望: 7 tests PASS

- [ ] **Step 6: Commit**

```bash
cd d:/Users/PC/Projects/if_mapping_gui
git add i18n/zh.json i18n/ja.json i18n.py tests/test_i18n.py
git commit -m "feat: add i18n module with zh/ja translation files"
```

---

## Task 2: 更新 config.py 和 BaseFrame

**Files:**
- Modify: `config.py`
- Modify: `gui/frames/__init__.py`

- [ ] **Step 1: 更新 config.py — 新增 ui_language 默认值**

在 `DEFAULTS` 字典中新增一行：

```python
DEFAULTS: dict = {
    "server_url": "http://localhost:4004",
    "provider": "claude",
    "language": "ja",
    "ui_language": "zh",      # 新增此行
    "timeout": 600,
    "last_input_dir": "",
}
```

- [ ] **Step 2: 更新 BaseFrame — 新增 retranslate() 默认方法**

```python
# gui/frames/__init__.py
import customtkinter as ctk

class BaseFrame(ctk.CTkFrame):
    """Base class for all content frames."""
    def __init__(self, master, app, **kwargs):
        super().__init__(master, **kwargs)
        self.app = app

    def retranslate(self) -> None:
        """Override in subclasses to refresh all static widget text."""
        pass
```

- [ ] **Step 3: 运行现有测试确认无回归**

```bash
cd d:/Users/PC/Projects/if_mapping_gui
pytest tests/ -v --ignore=tests/test_app_smoke.py
```

期望: PASS

- [ ] **Step 4: Commit**

```bash
git add config.py gui/frames/__init__.py
git commit -m "feat: add ui_language config default and BaseFrame.retranslate()"
```

---

## Task 3: 更新 App — 启动时加载语言，新增 retranslate()

**Files:**
- Modify: `gui/app.py`

- [ ] **Step 1: 更新 app.py**

```python
# gui/app.py
import customtkinter as ctk
import config
import i18n
from gui.frames.match_frame import MatchFrame
from gui.frames.upload_frame import UploadFrame
from gui.frames.prompts_frame import PromptsFrame
from gui.frames.logs_frame import LogsFrame
from gui.frames.settings_frame import SettingsFrame
from api.cap_client import CapClient

ctk.set_appearance_mode("dark")
ctk.set_default_color_theme("blue")

_NAV_KEYS = [
    ("match",   "nav.match"),
    ("upload",  "nav.upload"),
    ("prompts", "nav.prompts"),
    ("logs",    "nav.logs"),
]


class App(ctk.CTk):
    def __init__(self):
        super().__init__()
        self.cfg = config.load()
        i18n.load(self.cfg.get("ui_language", "zh"))

        self.title(i18n.t("app.title"))
        self.geometry("920x640")

        self._build_layout()
        self.show_frame("match")
        self.after(500, self._refresh_status)

    def _build_layout(self):
        self._statusbar = ctk.CTkFrame(self, height=28, corner_radius=0)
        self._statusbar.pack(side="bottom", fill="x")
        self._statusbar.pack_propagate(False)
        self._conn_label = ctk.CTkLabel(self._statusbar, text=i18n.t("status.checking"), font=("", 11))
        self._conn_label.pack(side="left", padx=12)
        ctk.CTkLabel(self._statusbar, text="v0.1.0", font=("", 10)).pack(side="right", padx=12)

        self._sidebar = ctk.CTkFrame(self, width=164, corner_radius=0)
        self._sidebar.pack(side="left", fill="y")
        self._sidebar.pack_propagate(False)

        self._app_title_label = ctk.CTkLabel(
            self._sidebar, text=i18n.t("app.title"), font=("", 13, "bold")
        )
        self._app_title_label.pack(pady=(18, 14), padx=10)

        self._nav_btns: dict[str, ctk.CTkButton] = {}
        self._nav_keys = _NAV_KEYS
        for key, label_key in _NAV_KEYS:
            btn = ctk.CTkButton(
                self._sidebar, text=i18n.t(label_key), anchor="w", height=34,
                command=lambda k=key: self.show_frame(k),
            )
            btn.pack(fill="x", padx=8, pady=2)
            self._nav_btns[key] = btn

        settings_btn = ctk.CTkButton(
            self._sidebar, text=i18n.t("nav.settings"), anchor="w", height=34,
            command=lambda: self.show_frame("settings"),
        )
        settings_btn.pack(fill="x", padx=8, pady=2, side="bottom")
        self._nav_btns["settings"] = settings_btn

        self._main = ctk.CTkFrame(self, corner_radius=0)
        self._main.pack(side="left", fill="both", expand=True)

        self._frames: dict[str, ctk.CTkFrame] = {
            "match":    MatchFrame(self._main, self),
            "upload":   UploadFrame(self._main, self),
            "prompts":  PromptsFrame(self._main, self),
            "logs":     LogsFrame(self._main, self),
            "settings": SettingsFrame(self._main, self),
        }
        for frame in self._frames.values():
            frame.place(relx=0, rely=0, relwidth=1, relheight=1)

    def show_frame(self, name: str) -> None:
        self._frames[name].tkraise()
        for k, btn in self._nav_btns.items():
            btn.configure(fg_color=("#0f3460", "#0f3460") if k == name else "transparent")

    def retranslate(self) -> None:
        self.title(i18n.t("app.title"))
        self._app_title_label.configure(text=i18n.t("app.title"))
        for key, label_key in self._nav_keys:
            self._nav_btns[key].configure(text=i18n.t(label_key))
        self._nav_btns["settings"].configure(text=i18n.t("nav.settings"))
        for frame in self._frames.values():
            frame.retranslate()

    def update_status(self, connected: bool) -> None:
        url = self.cfg.get("server_url", "")
        if connected:
            self._conn_label.configure(text=f"●  {url}", text_color="#22c55e")
        else:
            self._conn_label.configure(text=i18n.t("status.disconnected"), text_color="#ef4444")

    def get_client(self) -> CapClient:
        return CapClient(self.cfg.get("server_url", "http://localhost:4004"))

    def _refresh_status(self) -> None:
        import threading
        def _check():
            ok = self.get_client().ping()
            self.after(0, lambda: self.update_status(ok))
        threading.Thread(target=_check, daemon=True).start()
        self.after(30_000, self._refresh_status)
```

- [ ] **Step 2: Commit**

```bash
git add gui/app.py
git commit -m "feat: i18n App — load language on startup, add retranslate()"
```

---

## Task 4: 国际化 match_frame.py

**Files:**
- Modify: `gui/frames/match_frame.py`

- [ ] **Step 1: 更新 match_frame.py**

在文件顶部（现有 import 之后）添加：
```python
import i18n
```

将 `match_worker` 函数中的硬编码中文字符串替换为 `t()` 调用：

```python
def match_worker(
    files: list[str],
    provider: str,
    language: str,
    server_url: str,
    timeout: int,
    excel_cfg: dict,
    q: queue.Queue,
    stop: threading.Event,
) -> None:
    def log(text, level="info"):
        ts = datetime.now().strftime("%H:%M:%S")
        q.put({"type": "log", "text": f"[{ts}]  {level.upper():<5} {text}", "level": level})

    client = CapClient(server_url, timeout=timeout)
    if not client.ping():
        log(i18n.t("match.conn_fail_log", url=server_url), "error")
        q.put({"type": "error", "msg": "CAP service unreachable"})
        return

    correlation_id = uuid.uuid4().hex[:12]
    sse_stop = threading.Event()
    sse_thread = threading.Thread(
        target=_sse_worker,
        args=(client, correlation_id, q, sse_stop),
        daemon=True,
    )
    sse_thread.start()

    all_results: list[dict] = []
    all_wb: dict[str, openpyxl.Workbook] = {}
    all_row_indices: dict[str, set[int]] = {}
    all_directions: dict[str, str] = {}

    try:
        for i, file_str in enumerate(files):
            if stop.is_set():
                log(i18n.t("match.user_stop_log"), "warn")
                break

            path = Path(file_str)
            try:
                fields, workbook, direction = read_fields(path, excel_cfg)
                all_wb[file_str] = workbook
                all_row_indices[file_str] = {f.rowIndex for f in fields}
                all_directions[file_str] = direction
                log(i18n.t("match.parse_log", name=path.name, count=str(len(fields)), direction=direction))
            except ExcelReadError as e:
                log(str(e), "error")
                continue

            try:
                results = client.match(
                    [f.to_dict() for f in fields],
                    provider=provider,
                    language=language,
                    correlation_id=correlation_id,
                )
                all_results.extend(results)
                custom = sum(1 for r in results if r.get("matchSource") in ("exact", "vector"))
                ai     = sum(1 for r in results if r.get("matchSource") == "ai")
                log(i18n.t("match.match_done_log", total=str(len(results)), custom=str(custom), ai=str(ai)), "step")
            except Exception as e:
                log(i18n.t("match.match_fail_log", name=path.name, error=str(e)), "error")

            q.put({"type": "progress", "pct": (i + 1) / len(files)})

        q.put({
            "type": "done",
            "results": all_results,
            "wb": all_wb,
            "row_indices": all_row_indices,
            "directions": all_directions,
            "files": files,
        })
    finally:
        sse_stop.set()
```

将 `_build()` 方法中的硬编码文本替换：

```python
def _build(self):
    pad = {"padx": 16, "pady": 6}

    self._drop_zone = ctk.CTkButton(
        self, text=i18n.t("match.pick_files"),
        height=72, fg_color="#1e293b", hover_color="#334155",
        text_color="#64748b", command=self._pick_files,
    )
    self._drop_zone.pack(fill="x", **pad)

    self._file_list_frame = ctk.CTkScrollableFrame(self, height=80, fg_color="#1e293b")
    self._file_list_frame.pack(fill="x", padx=16, pady=(0, 6))

    opts = ctk.CTkFrame(self, fg_color="transparent")
    opts.pack(fill="x", padx=16, pady=(0, 6))
    self._provider_label = ctk.CTkLabel(opts, text=i18n.t("match.provider_label"), font=("", 10), text_color="gray")
    self._provider_label.grid(row=0, column=0, sticky="w")
    self._provider_var = ctk.StringVar(value=self.app.cfg.get("provider", "claude"))
    ctk.CTkOptionMenu(opts, variable=self._provider_var,
                      values=["claude", "openai", "gemini"], width=120).grid(row=1, column=0, padx=(0, 10))
    self._lang_label = ctk.CTkLabel(opts, text=i18n.t("match.lang_label"), font=("", 10), text_color="gray")
    self._lang_label.grid(row=0, column=1, sticky="w")
    self._lang_var = ctk.StringVar(value=self.app.cfg.get("language", "ja"))
    ctk.CTkOptionMenu(opts, variable=self._lang_var,
                      values=["ja", "en", "zh"], width=100).grid(row=1, column=1, padx=(0, 10))
    self._start_btn = ctk.CTkButton(opts, text=i18n.t("match.start_btn"), width=100, command=self._start)
    self._start_btn.grid(row=1, column=2, padx=(10, 6))
    self._stop_btn = ctk.CTkButton(opts, text=i18n.t("match.stop_btn"), width=80,
                                   fg_color="#1e293b", command=self._stop, state="disabled")
    self._stop_btn.grid(row=1, column=3)

    self._progress = ctk.CTkProgressBar(self)
    self._progress.set(0)
    self._progress.pack(fill="x", padx=16, pady=(0, 6))

    self._log = ctk.CTkTextbox(self, height=160, font=("Consolas", 10), state="disabled")
    self._log.pack(fill="both", expand=True, padx=16, pady=(0, 6))

    result_bar = ctk.CTkFrame(self, fg_color="transparent")
    result_bar.pack(fill="x", padx=16, pady=(0, 10))
    self._result_label = ctk.CTkLabel(result_bar, text="", font=("", 11), text_color="gray")
    self._result_label.pack(side="left")
    self._export_btn = ctk.CTkButton(result_bar, text=i18n.t("match.export_btn"),
                                      width=140, command=self._export, state="disabled")
    self._export_btn.pack(side="right")
```

将 `_pick_files()` 中的对话框标题替换：
```python
def _pick_files(self):
    paths = filedialog.askopenfilenames(
        title=i18n.t("match.pick_title"),
        filetypes=[("Excel files", "*.xlsx *.xls")],
        initialdir=self.app.cfg.get("last_input_dir") or None,
    )
    # ... 其余不变
```

将 `_start()` 中的错误消息替换：
```python
def _start(self):
    if not self._files:
        self._log_append(i18n.t("match.no_file_error"), "error")
        return
    # ... 其余不变
```

将 `_on_done()` 中的结果摘要替换：
```python
def _on_done(self):
    custom = sum(1 for r in self._results if r.get("matchSource") in ("exact", "vector"))
    ai     = sum(1 for r in self._results if r.get("matchSource") == "ai")
    self._result_label.configure(
        text=i18n.t("match.result_summary", total=str(len(self._results)), custom=str(custom), ai=str(ai))
    )
    # ... 其余不变
```

将 `_export()` 中的日志消息替换：
```python
self._log_append(i18n.t("match.export_done_log", name=out.name), "info")
```

新增 `retranslate()` 方法（放在 `_export` 之后）：
```python
def retranslate(self) -> None:
    self._drop_zone.configure(text=i18n.t("match.pick_files"))
    self._provider_label.configure(text=i18n.t("match.provider_label"))
    self._lang_label.configure(text=i18n.t("match.lang_label"))
    self._start_btn.configure(text=i18n.t("match.start_btn"))
    self._stop_btn.configure(text=i18n.t("match.stop_btn"))
    self._export_btn.configure(text=i18n.t("match.export_btn"))
```

- [ ] **Step 2: 运行相关测试**

```bash
cd d:/Users/PC/Projects/if_mapping_gui
pytest tests/test_match_worker.py -v
```

期望: PASS

- [ ] **Step 3: Commit**

```bash
git add gui/frames/match_frame.py
git commit -m "feat: i18n match_frame — all UI text via t()"
```

---

## Task 5: 国际化 upload_frame.py

**Files:**
- Modify: `gui/frames/upload_frame.py`

- [ ] **Step 1: 更新 upload_frame.py**

在顶部添加 `import i18n`。

将 `upload_worker` 中的硬编码字符串替换：
```python
def upload_worker(file_str, mode, server_url, q, stop, kb_cfg=None):
    def log(text, level="info"):
        ts = datetime.now().strftime("%H:%M:%S")
        q.put({"type": "log", "text": f"[{ts}]  {level.upper():<5} {text}", "level": level})

    client = CapClient(server_url)
    if not client.ping():
        log(i18n.t("upload.conn_fail_log", url=server_url), "error")
        q.put({"type": "error"})
        return

    path = Path(file_str)
    try:
        records = read_kb_fields(path, kb_cfg=kb_cfg)
        log(i18n.t("upload.parse_log", name=path.name, count=str(len(records))))
    except ExcelReadError as e:
        log(str(e), "error")
        q.put({"type": "error"})
        return

    try:
        result = client.upload_custom_fields(records, mode=mode)
        inserted = result.get("inserted", 0)
        updated = result.get("updated", 0)
        deleted = result.get("deleted", 0)
        log(i18n.t("upload.done_log", inserted=str(inserted), updated=str(updated), deleted=str(deleted)))
        q.put({"type": "done"})
    except CapConnectionError as e:
        log(i18n.t("upload.fail_log", error=str(e)), "error")
        q.put({"type": "error"})
```

将 `_build()` 中的硬编码文本替换（保存 widget 引用以便 retranslate）：
```python
def _build(self):
    pad = {"padx": 16, "pady": 8}

    self._title_label = ctk.CTkLabel(self, text=i18n.t("upload.title"), font=("", 16, "bold"))
    self._title_label.pack(anchor="w", padx=20, pady=(20, 4))

    self._drop_btn = ctk.CTkButton(
        self, text=i18n.t("upload.pick_btn"), height=60,
        fg_color="#1e293b", hover_color="#334155", text_color="#64748b",
        command=self._pick_file,
    )
    self._drop_btn.pack(fill="x", **pad)

    self._file_label = ctk.CTkLabel(self, text="", font=("", 11), text_color="#94a3b8")
    self._file_label.pack(anchor="w", padx=16)

    opts = ctk.CTkFrame(self, fg_color="transparent")
    opts.pack(fill="x", **pad)
    self._mode_label = ctk.CTkLabel(opts, text=i18n.t("upload.mode_label"), font=("", 10), text_color="gray")
    self._mode_label.pack(side="left", padx=(0, 6))
    self._mode_var = ctk.StringVar(value="upsert")
    ctk.CTkOptionMenu(opts, variable=self._mode_var,
                      values=["upsert", "overwrite"], width=160).pack(side="left", padx=(0, 12))
    self._upload_btn = ctk.CTkButton(opts, text=i18n.t("upload.upload_btn"), width=90, command=self._start)
    self._upload_btn.pack(side="left")

    self._log = ctk.CTkTextbox(self, height=160, font=("Consolas", 10), state="disabled")
    self._log.pack(fill="both", expand=True, padx=16, pady=(6, 16))
```

将 `_pick_file()` 中的对话框标题替换：
```python
def _pick_file(self):
    path = filedialog.askopenfilename(
        title=i18n.t("upload.pick_title"),
        filetypes=[("Excel files", "*.xlsx *.xls")],
        initialdir=self.app.cfg.get("last_input_dir") or None,
    )
    if path:
        self._file = path
        self._file_label.configure(text=f"📄 {Path(path).name}")
```

将 `_start()` 中的错误消息替换：
```python
def _start(self):
    if not self._file:
        self._log_append(i18n.t("upload.no_file_error"))
        return
    # ... 其余不变
```

新增 `retranslate()` 方法：
```python
def retranslate(self) -> None:
    self._title_label.configure(text=i18n.t("upload.title"))
    self._drop_btn.configure(text=i18n.t("upload.pick_btn"))
    self._mode_label.configure(text=i18n.t("upload.mode_label"))
    self._upload_btn.configure(text=i18n.t("upload.upload_btn"))
```

- [ ] **Step 2: Commit**

```bash
git add gui/frames/upload_frame.py
git commit -m "feat: i18n upload_frame — all UI text via t()"
```

---

## Task 6: 国际化 prompts_frame.py 和 logs_frame.py

**Files:**
- Modify: `gui/frames/prompts_frame.py`
- Modify: `gui/frames/logs_frame.py`

- [ ] **Step 1: 更新 prompts_frame.py**

在顶部添加 `import i18n`。

将 `_build()` 中硬编码文本替换（保存 widget 引用）：
```python
def _build(self):
    self._title_label = ctk.CTkLabel(self, text=i18n.t("prompts.title"), font=("", 16, "bold"))
    self._title_label.pack(anchor="w", padx=20, pady=(20, 8))

    main = ctk.CTkFrame(self, fg_color="transparent")
    main.pack(fill="both", expand=True, padx=16, pady=(0, 16))
    main.columnconfigure(1, weight=1)
    main.rowconfigure(1, weight=1)

    filter_row = ctk.CTkFrame(main, fg_color="transparent")
    filter_row.grid(row=0, column=0, sticky="ew", pady=(0, 6))
    self._lang_btns: dict[str, ctk.CTkButton] = {}
    for lang in ("ja", "en", "zh"):
        btn = ctk.CTkButton(filter_row, text=lang.upper(), width=44, height=26,
                            command=lambda l=lang: self._set_lang(l))
        btn.pack(side="left", padx=2)
        self._lang_btns[lang] = btn
    self._set_lang("ja")

    self._list_frame = ctk.CTkScrollableFrame(main, width=210)
    self._list_frame.grid(row=1, column=0, sticky="ns", padx=(0, 10))

    right = ctk.CTkFrame(main, fg_color="transparent")
    right.grid(row=0, column=1, rowspan=2, sticky="nsew")
    right.rowconfigure(1, weight=1)

    self._edit_title = ctk.CTkLabel(right, text="", font=("", 12, "bold"))
    self._edit_title.grid(row=0, column=0, sticky="w", pady=(0, 6))
    self._editor = ctk.CTkTextbox(right, font=("Consolas", 11))
    self._editor.grid(row=1, column=0, sticky="nsew")
    right.columnconfigure(0, weight=1)

    btn_row = ctk.CTkFrame(right, fg_color="transparent")
    btn_row.grid(row=2, column=0, sticky="e", pady=(8, 0))
    self._cancel_btn = ctk.CTkButton(btn_row, text=i18n.t("prompts.cancel_btn"), width=70,
                                     fg_color="#1e293b", command=self._cancel)
    self._cancel_btn.pack(side="left", padx=4)
    self._save_btn = ctk.CTkButton(btn_row, text=i18n.t("prompts.save_btn"), width=80, command=self._save)
    self._save_btn.pack(side="left", padx=4)
    self._reload_btn = ctk.CTkButton(btn_row, text=i18n.t("prompts.reload_btn"), width=80,
                                     fg_color="#1e293b", command=self._reload_server)
    self._reload_btn.pack(side="left", padx=4)

    self.bind("<Visibility>", lambda e: self._load_prompts())
    self._load_prompts()
```

新增 `retranslate()` 方法：
```python
def retranslate(self) -> None:
    self._title_label.configure(text=i18n.t("prompts.title"))
    self._cancel_btn.configure(text=i18n.t("prompts.cancel_btn"))
    self._save_btn.configure(text=i18n.t("prompts.save_btn"))
    self._reload_btn.configure(text=i18n.t("prompts.reload_btn"))
```

- [ ] **Step 2: 更新 logs_frame.py**

在顶部添加 `import i18n`。

将 `_build()` 中硬编码文本替换（保存 widget 引用）：
```python
def _build(self):
    header = ctk.CTkFrame(self, fg_color="transparent")
    header.pack(fill="x", padx=16, pady=(16, 8))
    self._title_label = ctk.CTkLabel(header, text=i18n.t("logs.title"), font=("", 16, "bold"))
    self._title_label.pack(side="left")
    self._refresh_btn = ctk.CTkButton(header, text=i18n.t("logs.refresh_btn"), width=80, command=self._load)
    self._refresh_btn.pack(side="right")

    chips = ctk.CTkFrame(self, fg_color="transparent")
    chips.pack(fill="x", padx=16, pady=(0, 12))
    self._input_label = self._chip(chips, "0", i18n.t("logs.total_input"))
    self._input_label.pack(side="left", padx=(0, 10))
    self._output_label = self._chip(chips, "0", i18n.t("logs.total_output"))
    self._output_label.pack(side="left", padx=(0, 10))
    self._calls_label = self._chip(chips, "0", i18n.t("logs.call_count"))
    self._calls_label.pack(side="left")

    self._header_row = ctk.CTkFrame(self, fg_color="#1e293b", height=28)
    self._header_row.pack(fill="x", padx=16)
    self._col_labels: list[ctk.CTkLabel] = []
    for col_key, w in [
        ("logs.col_time", 80), ("logs.col_provider", 70), ("logs.col_step", 120),
        ("logs.col_input", 70), ("logs.col_output", 70)
    ]:
        lbl = ctk.CTkLabel(self._header_row, text=i18n.t(col_key), font=("", 10, "bold"), width=w)
        lbl.pack(side="left", padx=4)
        self._col_labels.append((lbl, col_key))

    self._table = ctk.CTkScrollableFrame(self, fg_color="transparent")
    self._table.pack(fill="both", expand=True, padx=16, pady=(0, 16))

    self._load()
```

新增 `retranslate()` 方法，并保存 chip 的子标签引用（需稍作调整）。

由于 `_chip()` 方法创建的帧内部标签不易在 retranslate 中引用，最简单的方式是记录 chip 数据标签引用：

在 `_build` 中将 chip 的文字标签也保存引用：
```python
self._input_chip_label = self._chip(chips, "0", i18n.t("logs.total_input"))
self._input_chip_label.pack(side="left", padx=(0, 10))
# （self._input_label 也保持，用于更新数值）
self._input_label = self._input_chip_label
```

注意 `_chip()` 返回的是 frame 本身，其内部的文字标签存在 `frame._text_label` 上（需修改 `_chip`）。

修改 `_chip()` 方法，增加 `_text_label` 引用：
```python
def _chip(self, parent, value: str, label: str) -> ctk.CTkFrame:
    frame = ctk.CTkFrame(parent, fg_color="#1e293b", corner_radius=6)
    val_lbl = ctk.CTkLabel(frame, text=value, font=("", 18, "bold"), text_color="#7b8cde")
    val_lbl.pack(padx=14, pady=(8, 0))
    text_lbl = ctk.CTkLabel(frame, text=label, font=("", 10), text_color="gray")
    text_lbl.pack(padx=14, pady=(0, 8))
    frame._value_label = val_lbl
    frame._text_label = text_lbl    # 新增，供 retranslate 使用
    return frame
```

新增 `retranslate()` 方法：
```python
def retranslate(self) -> None:
    self._title_label.configure(text=i18n.t("logs.title"))
    self._refresh_btn.configure(text=i18n.t("logs.refresh_btn"))
    self._input_label._text_label.configure(text=i18n.t("logs.total_input"))
    self._output_label._text_label.configure(text=i18n.t("logs.total_output"))
    self._calls_label._text_label.configure(text=i18n.t("logs.call_count"))
    for lbl, col_key in self._col_labels:
        lbl.configure(text=i18n.t(col_key))
```

- [ ] **Step 3: Commit**

```bash
git add gui/frames/prompts_frame.py gui/frames/logs_frame.py
git commit -m "feat: i18n prompts_frame and logs_frame"
```

---

## Task 7: 国际化 settings_frame.py — 新增 UI 语言选项

**Files:**
- Modify: `gui/frames/settings_frame.py`

这是最复杂的 frame，因为需要新增 UI 语言选项，并在保存时触发 `app.retranslate()`。

- [ ] **Step 1: 更新 settings_frame.py**

在顶部添加 `import i18n`。

在 `_build()` 开头将 `outer` 的标题改为 `t()`，并保存引用：
```python
def _build(self):
    outer = ctk.CTkScrollableFrame(self)
    outer.pack(fill="both", expand=True)
    pad = {"padx": 20, "pady": 8}

    self._title_label = ctk.CTkLabel(outer, text=i18n.t("settings.title"), font=("", 16, "bold"))
    self._title_label.pack(anchor="w", padx=20, pady=(20, 4))
```

将「CAP 服务地址」部分替换（保存标签引用）：
```python
    url_frame = ctk.CTkFrame(outer, fg_color="transparent")
    url_frame.pack(fill="x", **pad)
    self._cap_url_label = ctk.CTkLabel(url_frame, text=i18n.t("settings.cap_url_label"), font=("", 11))
    self._cap_url_label.pack(anchor="w")
    row = ctk.CTkFrame(url_frame, fg_color="transparent")
    row.pack(fill="x")
    self._url_entry = ctk.CTkEntry(row, width=240)
    self._url_entry.insert(0, self.app.cfg.get("server_url", "http://localhost:4004"))
    self._url_entry.pack(side="left", padx=(0, 8))
    self._timeout_label = ctk.CTkLabel(row, text=i18n.t("settings.timeout_label"), font=("", 10), text_color="#64748b")
    self._timeout_label.pack(side="left")
    self._timeout_entry = ctk.CTkEntry(row, width=60)
    self._timeout_entry.insert(0, str(self.app.cfg.get("timeout", 600)))
    self._timeout_entry.pack(side="left", padx=(4, 8))
    self._test_conn_btn = ctk.CTkButton(row, text=i18n.t("settings.test_conn_btn"), width=110, command=self._test_conn)
    self._test_conn_btn.pack(side="left", padx=(0, 8))
    self._conn_status = ctk.CTkLabel(row, text="", font=("", 11))
    self._conn_status.pack(side="left")
```

将「Provider + 语言」部分替换（保存标签引用，新增 UI 语言下拉）：
```python
    opts_frame = ctk.CTkFrame(outer, fg_color="transparent")
    opts_frame.pack(fill="x", **pad)
    self._provider_label = ctk.CTkLabel(opts_frame, text=i18n.t("settings.provider_label"), font=("", 11))
    self._provider_label.grid(row=0, column=0, sticky="w")
    self._provider_var = ctk.StringVar(value=self.app.cfg.get("provider", "claude"))
    ctk.CTkOptionMenu(opts_frame, variable=self._provider_var,
                      values=["claude", "openai", "gemini"], width=140).grid(row=1, column=0, padx=(0, 20))
    self._lang_label = ctk.CTkLabel(opts_frame, text=i18n.t("settings.lang_label"), font=("", 11))
    self._lang_label.grid(row=0, column=1, sticky="w")
    self._lang_var = ctk.StringVar(value=self.app.cfg.get("language", "ja"))
    ctk.CTkOptionMenu(opts_frame, variable=self._lang_var,
                      values=["ja", "en", "zh"], width=140).grid(row=1, column=1, padx=(0, 20))
    self._ui_lang_label = ctk.CTkLabel(opts_frame, text=i18n.t("settings.ui_lang_label"), font=("", 11))
    self._ui_lang_label.grid(row=0, column=2, sticky="w")
    self._ui_lang_var = ctk.StringVar(value=self.app.cfg.get("ui_language", "zh"))
    ctk.CTkOptionMenu(opts_frame, variable=self._ui_lang_var,
                      values=["zh", "ja"], width=100).grid(row=1, column=2)
```

将「Excel 列配置」标题替换：
```python
    self._excel_label = ctk.CTkLabel(outer, text=i18n.t("settings.excel_section"), font=("", 13, "bold"))
    self._excel_label.pack(anchor="w", padx=20, pady=(16, 4))
```

在 `_labeled_entry` 内部函数中，将 label 参数改为直接传翻译后的字符串（调用处传 `i18n.t(key)`）：
```python
    self._sheet_head_entry  = _labeled_entry(global_frame, i18n.t("settings.sheet_head"), excel_cfg["sheet_head"])
    self._sheet_data_entry  = _labeled_entry(global_frame, i18n.t("settings.sheet_data"), excel_cfg["sheet_data"])
    self._header_row_entry  = _labeled_entry(global_frame, i18n.t("settings.header_row"), excel_cfg["header_row"], width=60)
    self._start_row_entry   = _labeled_entry(global_frame, i18n.t("settings.start_row"),  excel_cfg["start_row"],  width=60)
```

```python
    self._det_col_entry     = _labeled_entry(det_frame, i18n.t("settings.det_col"),     det["col"],     width=52)
    self._det_row_entry     = _labeled_entry(det_frame, i18n.t("settings.det_row"),     det["row"],     width=60)
    self._det_keyword_entry = _labeled_entry(det_frame, i18n.t("settings.det_keyword"), det["keyword"], width=80)
```

将 Tab 标题替换（customtkinter CTkTabview 不支持运行时修改标题，所以此处在 build 时用 t() 即可，retranslate 时不需处理）：
```python
    tab.add(i18n.t("settings.dir_normal"))
    tab.add(i18n.t("settings.dir_sap"))

    for direction, tab_key, hkey, rkey, okey in [
        ("normal", "settings.dir_normal", "normal_header", "normal_row", "normal_output"),
        ("sap",    "settings.dir_sap",    "sap_header",    "sap_row",    "sap_output"),
    ]:
        dir_cfg = excel_cfg["directions"][direction]
        scroll = ctk.CTkScrollableFrame(tab.tab(i18n.t(tab_key)), height=340)
        scroll.pack(fill="both", expand=True)
        _build_col_grid(scroll, i18n.t("settings.input_header_cols"), dir_cfg["input_header_cols"], self._excel_entries[hkey])
        _build_col_grid(scroll, i18n.t("settings.input_row_cols"),    dir_cfg["input_row_cols"],    self._excel_entries[rkey])
        _build_col_grid(scroll, i18n.t("settings.output_cols"),       dir_cfg["output_cols"],       self._excel_entries[okey])
```

将保存按钮替换：
```python
    self._save_settings_btn = ctk.CTkButton(outer, text=i18n.t("settings.save_btn"), width=120, command=self._save)
    self._save_settings_btn.pack(anchor="e", padx=20, pady=12)
```

在 `_test_conn()` 中替换状态文本：
```python
def _test_conn(self):
    url = self._url_entry.get().strip()
    self._conn_status.configure(text=i18n.t("settings.testing"), text_color="gray")

    def _check():
        from api.cap_client import CapClient
        ok = CapClient(url).ping()
        self.after(0, lambda: self._conn_status.configure(
            text=i18n.t("settings.connected") if ok else i18n.t("settings.disconnected"),
            text_color="#22c55e" if ok else "#ef4444",
        ))

    threading.Thread(target=_check, daemon=True).start()
```

在 `_save()` 方法末尾（`config.save` 调用后）添加 UI 语言保存和重绘逻辑：
```python
def _save(self):
    self.app.cfg["server_url"] = self._url_entry.get().strip()
    self.app.cfg["provider"]   = self._provider_var.get()
    self.app.cfg["language"]   = self._lang_var.get()
    new_ui_lang = self._ui_lang_var.get()
    self.app.cfg["ui_language"] = new_ui_lang
    try:
        self.app.cfg["timeout"] = int(self._timeout_entry.get())
    except ValueError:
        pass

    # ... Excel cfg 部分与原代码相同，不变 ...

    config.save(self.app.cfg)

    if new_ui_lang != i18n.current():
        i18n.load(new_ui_lang)
        self.app.retranslate()

    self.app.update_status(False)
    self.app._refresh_status()
```

新增 `retranslate()` 方法（Tab 标题无法动态更新，其余静态标签更新）：
```python
def retranslate(self) -> None:
    self._title_label.configure(text=i18n.t("settings.title"))
    self._cap_url_label.configure(text=i18n.t("settings.cap_url_label"))
    self._timeout_label.configure(text=i18n.t("settings.timeout_label"))
    self._test_conn_btn.configure(text=i18n.t("settings.test_conn_btn"))
    self._provider_label.configure(text=i18n.t("settings.provider_label"))
    self._lang_label.configure(text=i18n.t("settings.lang_label"))
    self._ui_lang_label.configure(text=i18n.t("settings.ui_lang_label"))
    self._excel_label.configure(text=i18n.t("settings.excel_section"))
    self._save_settings_btn.configure(text=i18n.t("settings.save_btn"))
```

- [ ] **Step 2: Commit**

```bash
git add gui/frames/settings_frame.py
git commit -m "feat: i18n settings_frame — add UI language selector, trigger retranslate on save"
```

---

## Task 8: 全面回归测试

- [ ] **Step 1: 运行所有非 GUI 测试**

```bash
cd d:/Users/PC/Projects/if_mapping_gui
pytest tests/ -v --ignore=tests/test_app_smoke.py
```

期望: 全部 PASS

- [ ] **Step 2: 手动验证 GUI（可选，需要桌面环境）**

```bash
python gui_main.py
```

验证清单：
1. 启动时默认显示中文
2. 进入「设置」→「界面语言」切换为 `ja` → 点「保存设置」→ 所有 frame 文字变为日语
3. 再次切换回 `zh` → 文字恢复中文
4. 重启应用，确认语言设置持久化

- [ ] **Step 3: 最终 commit（如有遗漏修复）**

```bash
git add -A
git commit -m "feat: complete i18n for if_mapping_gui — zh/ja support"
```

---

## 自查清单

- [x] **Spec 覆盖**: 翻译文件 ✓，i18n.py ✓，config.py ui_language ✓，BaseFrame.retranslate() ✓，App.retranslate() ✓，全部 5 个 frame ✓，设置页面 UI 语言选项 ✓，保存后即时切换 ✓
- [x] **占位符扫描**: 无 TBD/TODO
- [x] **类型一致性**: `t(key, **kwargs)` 在所有调用处一致；`retranslate()` 方法名在 BaseFrame、App、所有 frame 中一致；`_text_label` 引用在 `_chip()` 和 `retranslate()` 中一致
